---
description: 仅核心复杂代码才用第三方强模型（控制成本）
alwaysApply: true
---

# 第三方强模型：成本优先

## 默认

**默认用当前会话模型完成工作。** 不要为了「更稳」就开 `Task` + Opus/GPT。

跑测试、改文案、更新 `PROGRESS.md`、接 UI/invoke、查缺口、小修 bug、格式化、对照 docs 做 review——一律本会话直接做。

## 仅这些「核心复杂代码」才用第三方强模型

只有同时满足下面两点时，才通过 `Task` 交给强模型：

1. **核心路径**：鉴权/安全边界、录音采集与上传状态机、Egress 编排与媒体终态、真 ASR/索引管线等，出错代价高或难事后修补；
2. **实现难度高**：需要新设计、多模块算法/协议细节、或大段从零编写（不是接线、改签名、补测试）。

典型**要**用强模型：

- 新写或大改 cpal 采集会话 / 断点续传 / spool 加密
- 新写或大改 Egress 生命周期与终态语义
- 新接 FunASR / Vespa ACL 等会后核心管线
- 鉴权、上传校验等安全敏感协议的**首次**正确实现

典型**不要**用强模型（省成本）：

- 把已有模块接到 `main` / HTTP / RoomPage
- 补单测、修编译、改 feature 名、删重复代码
- 更新文档与进度、缺口 review、跑 `go test` / `cargo test`
- 样式、按钮、文案、环境变量示例
- 「再 review 一遍」——除非用户明确要求且范围是上述核心代码

## 模型选择（仅在触发时）

1. `claude-opus-5-thinking-high`（首选）
2. `gpt-5.6-sol-high`（备选）
3. `claude-opus-4-8-thinking-high`（若 5 不可用）

一次任务**尽量只开一个**强模型子代理；禁止并行多个强模型抢改同一批文件。实现完成后的合并、验证、PROGRESS 更新由主会话完成，不再为 review 单独开第二轮强模型（除非用户明确要求）。

## 工作方式

1. 先判断：这是「接线/验证」还是「核心新实现」？拿不准则**先用本会话**。
2. 确需强模型时：写清目标与验收标准，限定目录，禁止顺带改无关文件。
3. 子代理返回后：主会话合并、跑测试、更新进度。
